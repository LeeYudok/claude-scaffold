# Memory Index

This project's auto-memory SSOT is `.claude/memory/` (the system default path is not used).
Type prefixes: `project_`/`feedback_`/`reference_`/`user_`. Only `user_*` is personal; everything else is shared with the team.

<!-- Add one line per new memory below:
## Project
- [Title](project_xxx.md) — one-line hook
-->
